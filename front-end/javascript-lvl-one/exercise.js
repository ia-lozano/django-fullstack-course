// For this exercise we're going to make a lb to kg converter

// 1. Asking the user what the weight in lbs is:

var lbs = prompt('Enter the weight in lb:', 0);

// 2. Making the conversion from lb to kg:

var kgs = lbs*0.454;

// 3. Alerting the user what the actual output is:

alert('The weight in kg is: ' + kgs)